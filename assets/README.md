Place local GIF files in this directory and encode the filename in a QR code.

Examples:
- `cat.gif`
- `holograms/orb.gif`

The app will first try `assets/<qr-value>` and will also accept a full asset
key such as `assets/cat.gif`.
